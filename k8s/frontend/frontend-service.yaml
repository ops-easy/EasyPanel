apiVersion: v1
kind: Service
metadata:
  name: kube-bt-sync-frontend
  namespace: kube-bt-sync
  labels:
    app.kubernetes.io/name: kube-bt-sync-frontend
    app.kubernetes.io/part-of: kube-bt-sync
    app.kubernetes.io/component: frontend
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: kube-bt-sync-frontend
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 32080
