{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kube-bt-sync.serviceAccountName" . }}
  labels:
    {{- include "kube-bt-sync.labels" . | nindent 4 }}
{{- end }}
