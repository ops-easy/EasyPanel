backend:
  replicaCount: 1

  image:
    repository: ghcr.io/ops-easy/kube-bt-sync
    pullPolicy: IfNotPresent
    tag: "latest"

  service:
    type: ClusterIP
    port: 8080
    nodePort: 32080

  podSecurityContext:
    fsGroup: 65532
    runAsNonRoot: true
    runAsUser: 65532
    runAsGroup: 65532

  securityContext: {}

  resources:
    limits:
      cpu: "1"
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 128Mi

  env:
    KUBEBT_DATA_DIR: "/data"

  extraEnv: []
  #  - name: BAOTA_URL
  #    value: "http://..."
  #  - name: DDNS_HOST
  #    value: "home.example.com"

  # 敏感信息建议用 extraEnvFrom
  extraEnvFrom: []
  #  - secretRef:
  #      name: kube-bt-sync-secrets

  nodeSelector: {}
  tolerations: []
  affinity: {}

frontend:
  replicaCount: 1

  image:
    repository: ghcr.io/ops-easy/kube-bt-sync-web
    pullPolicy: IfNotPresent
    tag: "latest"

  service:
    type: NodePort
    port: 80
    nodePort: 32080

  resources:
    limits:
      cpu: 500m
      memory: 256Mi
    requests:
      cpu: 50m
      memory: 64Mi

  podSecurityContext: {}
  securityContext: {}
  nodeSelector: {}
  tolerations: []
  affinity: {}

  nginx:
    proxyReadTimeout: 3600s
    proxySendTimeout: 3600s

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  name: ""

ingress:
  enabled: false
  className: nginx
  annotations: {}
  hosts:
    - host: kube-bt-sync.example.com
      paths:
        - path: /
          pathType: Prefix
  tls: []

persistence:
  enabled: true
  storageClass: ""
  accessMode: ReadWriteOnce
  size: 5Gi

rbac:
  # true = 与 k8s/backend/rbac.yaml 一致（控制台 + YAML 下发）；false = 仅 Ingress 同步（需自行改模板）
  full: true
